# build with:
docker build -t ops-keystone .

# run with:
docker run -d -p 5000:5000 -p 35357:35357 ops-keystone

# (or in the foreground with)
docker run -it -p 5000:5000 -p 35357:35357 ops-keystone

# to use it with openstack commands from your host
1. Install openstack:

   sudo easy_install pip                      # to get a recent version of pip
   sudo pip install --upgrade pbr             # to upgrade the old version of pbr that comes with pip
   sudo pip install python-openstackclient    # to install a modern version of the openstack client

2. source openstack.osrc

3. Issue commands, such as:
   openstack token issue
