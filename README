### run with:
docker-compose up -d

# (or in the foreground with)
docker-compose up

# to use it with openstack CLI from your host
1. Install python-openstackclient (using your distro's package manager)

2. source openstack.osrc

3. Issue commands, such as:
   openstack token issue
